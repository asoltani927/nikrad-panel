/**
 * TASKS: @reza 
 * 1. Create a public route to make search for prodcuts and sellers using dokamerce sdk (it is a public route without login for home page) it will be use in command dialog (search) on the header *
 */
